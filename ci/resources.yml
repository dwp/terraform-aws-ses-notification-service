resources:
- name: terraform-aws-ses-notification-service-pr
  type: pull-request
  source:
    access_token: ((dataworks-secrets.concourse_github_pat))
    repository: dwp/terraform-aws-ses-notification-service
  check_every: 5m
  webhook_token: ((dataworks.concourse_github_webhook_token))

- name: terraform-aws-ses-notification-service-update-pipeline
  type: git
  source:
    branch: master
    access_token: ((dataworks-secrets.concourse_github_pat))
    uri: https://github.com/dwp/terraform-aws-ses-notification-service.git
    paths:
      - ci/*
      - aviator.yml
  check_every: 5m
  webhook_token: ((dataworks.concourse_github_webhook_token))
